{
  "name": "MFC Dev Container",
  "image": "mohdsaid497566/mfc:v4.4.1-cpu",
  "workspaceFolder": "/workspaces/${{repo}}",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash",
    "editor.formatOnSave": true
  },
  "postCreateCommand": "apt-get update && apt-get install -y curl && echo 'MFC Dev Container ready!'",
  "postStartCommand": "cd /opt/MFC && echo 'Welcome to the MFC Dev Container!'"
}